server:
  port: 8089
spring:
  application:
    name: oauth2-service
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/m1
    username: root
    password: '!Qazxsw2'
secure:
  ignored:
    urls: #安全路径白名单
      - /oauth2/**
      - /login/**
      - /logout/**
